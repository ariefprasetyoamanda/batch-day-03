const config = {
    env : process.env.NODE_ENV || 'dev',
    port : 3000,
    db_name : 'postgres',
    db_username : 'postgres',
    db_password : 'arief',
    db_port : 5432,
}
export default config